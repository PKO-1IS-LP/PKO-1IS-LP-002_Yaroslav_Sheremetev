# 002
Все задачи представлены в .py файлах

## Git команды
```
$ git init
$ git add .
$ git commit -m "Initial commit"
$ git branch -M main
$ echo "# 002" > README.md
$ git remote add origin https://github.com/PKO-1IS-LP/PKO-1IS-LP-002_Yaroslav_Sheremetev.git
$ git push -u origin main
```
